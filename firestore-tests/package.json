{
  "name": "crs-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Security-rules tests, run against the Firestore emulator.",
  "scripts": {
    "test": "cd .. && ./firestore-tests/node_modules/.bin/firebase emulators:exec --project crs-rules-test --only firestore \"node firestore-tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.0.0"
  }
}
